- [x] Update `gamification` table schema if needed, or rely on existing JSONB for badges.
- [x] Create `/api/student/leaderboard` endpoint.
- [x] Update `/api/student/dashboard` to calculate average communication score from `ai_analytics` and parse badges.
- [x] Create `/api/admin/overview` endpoint for system analytics (total students, active sessions, avg scores).
- [x] Create `/api/admin/sessions` endpoint to fetch sessions with participant counts (e.g. 44 students, 10 groups).
- [x] Update `Leaderboard.tsx` to fetch from `/api/student/leaderboard`.
- [x] Update `StudentDashboard.tsx` to display real Communication Score and Badges.
- [x] Update `AdminOverview.tsx` to fetch from `/api/admin/overview`.
- [x] Update `AdminSessions.tsx` to fetch from `/api/admin/sessions`.
- [x] Create `seed_admin_data.py` to populate DB with enough data so Admin dashboard looks realistic (multiple students, sessions, analytics).
